import p5 from "p5";

const sketch = (s: p5) => {
  const setup = () => {
    s.createCanvas(s.windowWidth, s.windowHeight);

    s.noStroke();
  };

  const draw = () => {
    s.background(0);
  };

  s.setup = setup;
  s.draw = draw;
};

export default sketch;
